# A²E Engine - Claude Code Instructions

## Project Overview
**Client:** Emmanuel (TokenOS)
**Platform:** compute.tokenos.ai
**Project:** A²E (Arbitrage & Orchestration Engine) for decentralized GPU compute

## Critical Rules

### Code Quality Standards
- All code must be **production-ready** — no prototype-quality, no "good enough for now"
- Follow industry best practices for Node.js/TypeScript development
- Proper error handling with meaningful error messages
- Input validation on all API endpoints
- No hardcoded values — use environment variables and config files
- Write clean, readable, self-documenting code
- Use meaningful variable and function names
- Keep functions small and single-purpose
- Add JSDoc comments for public APIs and complex logic only

### Git Commit Rules
- **NEVER** include "Co-Authored-By: Claude" or any AI attribution in commits
- **NEVER** include "Generated by Claude", "AI-generated", or similar references
- **NEVER** mention Claude, ChatGPT, Copilot, or any AI tool in commit messages
- Write commit messages as if written by a human developer
- Use conventional commit format: `feat:`, `fix:`, `refactor:`, `docs:`, `test:`

### Code Comments Rules
- **NEVER** include comments like "Generated by AI", "Claude suggestion", etc.
- **NEVER** leave TODO comments referencing AI tools
- **NEVER** include placeholder comments like "// AI: explain this"
- Comments should explain **why**, not **what** (code explains what)
- Remove any auto-generated boilerplate comments

### Testing Requirements
- Write unit tests for all business logic (especially routing/arbitrage)
- Write integration tests for API endpoints
- Test edge cases: yield floor boundaries, market failures, node timeouts
- Aim for >80% code coverage on core A²E logic

### Security Standards
- No secrets in code — use environment variables
- Validate all external input
- Sanitize data before database operations
- Use parameterized queries (Prisma handles this)
- Rate limiting on public endpoints
- CORS configured properly

## Tech Stack (Confirmed)
- **Backend:** Node.js + Fastify + TypeScript
- **Database:** PostgreSQL + Prisma
- **Queue:** BullMQ + Redis
- **Real-time:** Socket.io
- **Frontend:** Next.js 14 + React + Vite + Tailwind
- **Orchestration:** Kubernetes + Docker
- **Process Manager:** PM2

## Architecture Notes
- A²E is a **standalone microservice** — does not touch compute.tokenos.ai codebase
- Single integration point: `POST /route` endpoint
- Config-based market enabling — add credentials to config, market goes live
- No third-party monitoring tools (no Grafana/Prometheus) — custom dashboards only

## A²E Core Logic
```
Route to HIGHEST PAYING market for operators (NOT cheapest for buyers)

if (externalRate > internalRate) → route external
else if (internalRate >= yieldFloor) → route internal
else → route internal, enforce yield floor
```

## TokenOS Platform Rates (from compute.tokenos.ai)

### Internal Daily Yields (Revenue)
| GPU | Daily Yield | Hourly Rate |
|-----|-------------|-------------|
| H100 | $140.15/day | $5.84/hr |
| H200 | $179.85/day | $7.49/hr |
| B200 | $321.10/day | $13.38/hr |
| B300 | $431.75/day | $17.99/hr |
| GB300 | $499.35/day | $20.81/hr |

### Cost Floor (Wholesale)
| GPU | Daily Cost | Hourly Cost |
|-----|------------|-------------|
| H100 | ~$83/day | ~$3.46/hr |
| H200 | ~$105/day | ~$4.38/hr |
| B200 | ~$170/day | ~$7.08/hr |

### Platform Stats (as of 2026-03-22)
- 105 active nodes
- $846,078 total earnings paid
- 99.9% uptime
- Payments: USD → SOL (Solana), on-chain via Solscan

## Client Sensitivities
- Client is **very concerned about AI-generated code** — all work must appear professionally human-written
- Client values **honesty and transparency**
- Client has a **tight deadline** — no unnecessary delays
- Client expects **daily updates** on progress

## File Structure (Phase 1)
```
/a2e-engine
├── apps/
│   ├── api/              # Fastify backend
│   ├── dashboard/        # Next.js admin dashboard
│   └── node-agent/       # Agent running on GPU nodes
├── packages/
│   ├── core/             # A²E routing logic
│   ├── database/         # Prisma schema & client
│   ├── market-adapters/  # Akash, IO.net adapters
│   └── shared/           # Shared types & utilities
├── docker/
├── k8s/
└── docs/
```

## Deployment

### Infrastructure
| Component | Details |
|-----------|---------|
| Host Server | 135.181.162.188 (Hetzner Proxmox VE) |
| LXC Container | VMID 300 "tokenos-demo", IP 10.10.10.198 |
| Domain | tokenos.byredstone.com |
| Process Manager | PM2 (process: a2e-backend) |

### Deployment Workflow (MUST FOLLOW)
1. **Develop locally** — all coding happens on local machine
2. **Push to GitHub** — `git push origin main`
3. **SSH to server** — `ssh root@135.181.162.188`
4. **Enter LXC** — `pct enter 300` or SSH to 10.10.10.198
5. **Pull changes** — `cd /opt/a2e && git pull origin main`
6. **Restart service** — `pm2 restart a2e-backend`

### Server Commands
```bash
# SSH to host
ssh root@135.181.162.188

# Enter LXC container
pct enter 300

# Inside LXC - load NVM first
source /root/.nvm/nvm.sh

# PM2 commands
pm2 status
pm2 restart a2e-backend
pm2 logs a2e-backend
```

### GitHub Repository
- **Repo:** github.com/redstoneai/a2e-engine (PRIVATE)
- **Branch:** main
- **NEVER** commit directly to server — always push via GitHub

## Project Notes
- Detailed project notes are in `/notes/` directory (NOT tracked in git)
- Contains: client conversations, handoff docs, proposal PDFs
- If session is lost, read `/notes/PROJECT_NOTES.md` for full context

## Milestones
| Milestone | Scope | Payment |
|-----------|-------|---------|
| M1 | Architecture & Design | $800 |
| M2 | Node Registry, Orchestration & A²E Engine | $1,100 |
| M3 | Accounting, Yield & Admin Dashboard | $1,100 |
