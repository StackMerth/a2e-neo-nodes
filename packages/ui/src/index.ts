// @a2e/ui: shared design system for A2E platform apps.
//
// M1 ships only the design tokens (this barrel export). M4 will add
// component primitives (Button, Card, Table, Modal, Sidebar,
// CommandPalette, StatusDot, etc.) on top of the same token system.

export * from './tokens'
